<%= rails_env %>:
  secret_key_base:      '<%= ask(" *  Secret Key Base: ", SecureRandom.hex(64)) %>'
  devise_secret:        '<%= ask(" *    Devise Secret: ", SecureRandom.hex(64)) %>'
  dragonfly_secret:     '<%= ask(" *    Dragonfly Secret: ", SecureRandom.hex(64)) %>'
  s3_bucket_name:       '<%= ask(" *    S3 Bucket Name: ") %>'
  s3_access_key_id:     '<%= ask(" *    S3 Access Key ID: ") %>'
  s3_secret_access_key: '<%= ask(" *    S3 Secret Access Key: ") %>'
